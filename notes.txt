### TO DO STILL ###

    # add choices to the --target-rank and --derep-rank parameters in the parser, based on the RANKS list in tax_ranks.py

    # bring over the download_with_tqdm updates from bit (stream function gets content length itself)

    # pursue faster and better handling of reading and writing my run_data object when passing around to/from snakemake calls

    # add my with_spinner() from bit for things like extracting the tarballs (e.g., get-kofamscan-data, "Decompressing" in get-pfam-data, etc.)

    # make gtt an entry point with subcommands for all the gtt scripts (maybe)
        # `gtt data` (at least), mirroring `bit data` would be nice to have i think

    # still need to implement iToL-file generation for both pfam and ko searching results
        # the per-genome rules in the snakefiles (search-kos.smk and search-pfams.smk) might be the best place to start the info needed for this (if it's not already done)
            # actually it's probably just easier to read in the <output_dir>/ko-search-results/KO-hit-counts.tsv table and make it based on that
        # be sure to take into account if we are modifying the genome names so the iToL files match the final alignment/tree files
        # make a helper function that does this, and it will take just the path to the table and if it's for KOs or PFams
            # if they have different formats it'd need a KO or PFam designation. If they are the same format, maybe not

    # go through general.py GenomeData and RunData classes and see which methods i put in that i'm not using anywhere anymore

    # incorporate latest changes from master GToTree
        # gtt-gen-SCG-HMMs has been updated a bit (v1.8.17)
        # bit's way of downloading assemblies with increased robustness (maybe even drop snakemake for that phase?)
        # check for others...

    # modularize and reduce code redundancy in my user-side standalone scripts like:
        # gtt-get-accessions-from-GTDB (use what we can from utils/gtdb/get_gtdb_data.py; also see what i've done in bit already)
            # bring over bit-get-accessions-from-ncbi also
        # gtt-gen-SCG-HMMs (don't forget to include updates made to the v1 GToTree)
        # gtt-pfam-search (if keeping this, basically dump the v1 standalone script and start re-building from what's available already in v2)


    # figure out all helper scripts that are no longer needed and remove them

    # modularize and reduce code redundancy in any of my helper scripts still remaining

    # when 0 accessions are found/make it through, at that stage summary the message is like "0 of total found...", it should probably explicitly check and say None
        # this is probably the case for all input-processing stages

    # fix entry points/console scripts as needed in pyproject.toml

    # wherever I have this:
        # with tarfile.open(taxdump_path) as tarball:
            # tarball.extractall(location)
    # do this to get rid of warning message:
        # with tarfile.open(tarball_path) as tarball:
            # tarball.extractall(path=location, filter=lambda ti: ti)


    # add FAQ page about differences between GToTree v1 and v2

    # create new SCG-sets for all main phyla (and proteos/pseudomonadota) of gtdb (and archaea/bacteria)
        # continue hosting them as github assets
        # make an organized output for gtt-hmms that breaks done by domain for easier reading and selection

    # switch to only using http when available
        # affects:
            # get_ncbi_assembly_tables.py (DONE)
            # ncbi assembly downloads (DONE)
            # kofamscan data is only available via ftp (maybe host this somewhere else for GToTree; if it rarely changes)
                # looks like it does update regularly, i might just have to catch and report when this download fails and say it might be because it's only available via ftp
                # maybe it's small enough i can github asset this too? Then i could have it auto-update in there every so often like ncbi assembly summaries

    # still need to implement nucleotide mode?

    # if it's a re-run, and a genome was dropped later due to, e.g., not enough hit targets, the preprocessing summary is misleading as it says the genome was dropped earlier

    # add a flag when all is done, so when resume is tried, it says nothing to do

    # if genome filtering drops all genes of a given SCG-set, things might break
        # build some guard for that

    # do i want to modify 'reason-removed' for SCGs to delineate bewteen 'no/multiple hits found'
        # and dropped due to length filtering?

    # more unit tests once things are more decided and stable

    # sync the CHANGELOG.md with the latest in the master gtotree v1 branch
        # including needed updates that were done in the v1 branch that were not in the v2 branch :(


### other things to do prior to final launch ###
    # think about bioconda recipe, that will probably still need to be maintained separately from the conda-recipe in this repo
    # revisit all pages on the gtotree wiki and update them as needed
    # made the CHANGELOG.md (and release notes) really comprehensive about the v1 to v2 changes, and put a link to the initial release notes somewhere on the wiki maybe


# example tests
rm -rf gtotree-output/ ; gtotree -a ncbi-accessions.txt -H Universal

gtotree -a ncbi_accessions.txt -g genbank_files.txt -A amino_acid_files.txt -f fasta_files.txt -H Universal -t -P

GToTree -a ncbi_accessions.txt -g genbank_files.txt -A amino_acid_files.txt -f fasta_files.txt -H Universal -j 4 -t -P -F

gtotree -a ncbi_accessions.txt -g GToTree-test-data/genbank_files.txt -f GToTree-test-data/fasta_files.txt -A GToTree-test-data/amino_acid_files.txt -t -P

gtotree -a problem-accs.txt -o new-problem-accs -H Universal -d -G 0.1 -r 0.3 -F

cat problem-accs.txt
# GCA_902755425.1
# GCA_902785715.1
# GCA_902777105.1
# GCA_902765875.1
# GCA_016291275.1


### OVERALL NOTES ###
# add a stop at "Summary of input-genome preprocessing" to check there are 3 or more as needed to make a tree
# eventually look into putting a block for this prodigal failure: "at least 100000 bases for training." (search original files)
    # maybe just make a check that input genomes are greater than 100,000 bases (if that consistently triggers it)
# eventually add a check that nucleotide fasta inputs are nucleotide and not amino acid, and vice versa

### TO DO ###

### Overview of change from v1 to v2 ###
# The codebase has been virtually entirely rewritten from an 5,000 line bash runner and X disparate
# python scripts for a total of XX,XXX lines of code into a (admittedly still poorly engineered and packaged)
# python package now with XXX modules and snakefiles over XX,XXX lines of code.

# Why did i do this? I don't know. It worked fine and I've successfully maintained it for 6 years and over 600 citations.

# But i felt it might help me grow...


### ADDED ###

# --taxon parameter added
    # this allows specifying a taxon at any rank in order to make a tree of genomes from that taxon (will only use species representatives)
    # this is so the user no longer needs to collect their own accession input lists anymore
# added a gene-represenation cutoff (-g) parameter
    # this is the minimum proportion of genomes required to have hits to a target gene for that target gene to be retained for downstream processing
    # e.g., with 100 input genomes and a -g of the default 0.2, any target gene that has hits in less than 20 genomes will be filtered out
    # This is calculated based on the total number of genomes remaining
    #   after preprocessing steps, but before genome filtering based on the '-G'
    #   parameter, and it is not revisited after genome-level filtering. This
    #   is to avoid iterative pruning effects between the '-g' and '-G' parameters.
# --resume flag added to attempt to continue interrupted runs

# lots of things since initial v1 release, but were integrated before v2

### CHANGED ###

# the code-base has been virtually entirely rewritten from
# when calculating percent of target genes required to be found (-X parameter)
    # now it uses the amount remaining after filtering out target genes with too few genomes having hits as the denominator
    # previously it used the starting total as the denominator
        # example: if starting with 100 target genes, and 20 are filtered out for having hits in too few genomes, then the percent required is based on 80 target-genes now rather than 100
# default number of jobs set to 4 instead of 1 (-j parameter)
# default output changed to "gtotree-output"
# dropped '-n' num HMM cpus argument (for scanning individual genomes, the default of 2 is virtually always fine)
# default lineage settings are D,P,C,S (no longer strain)
# printed output has more progress bars and fewer text printouts
# better logging of many steps
# http is used instead of ftp always where available (used to be ftp by default with an http option)
# output files and dirs that had underscores have had them replaced with dashes (sorry, i now hate underscores when they're not necessary)
# estimated percent completion and redundancy are no long provided
    # (this was always just "free" info, but checkm2 is a much better way to do this, so i made the decision to stop providing it at all)
# taxids are no longer searched for in input genbank files, they will only be found/used for provided input ncbi-accessions
    # due to the nature of genbank files, this was always a little messy and hacky. If this is actually a feature that benefitted anyone, i'll try adding it back in upon request :)
# the order of processing changed slightly from v1 to v2, so the output report messages vary a little
    # in v1, each genome was downloaded/parsed and scanned for the target genes in one pass
    # in v2, all input genomes are prepared for each input source, then all are searched together
# there are no longer separate summary tables for each input-genome source (accessions, fasta files, genbank files, and amino-acid files)
    # they are all represented in the primary "genomes-summary-info.tsv" table, which includes a "source" column
# references are slimmed down and hosted on GToTree github as assets now, this will help users who had trouble accessing ftp or other sites due to firewalls or other restrictions


## Fixes
### PLACES TO DEF IMPROVE ###


### IMPROVEMENTS to highlight in paper ###

# users no longer need to gather their own input accessions, they can just specify a taxon and GToTree will get the accessions for them
# python packaging and structure (rather than 4,000 lines of bash as main runner with dozens of individual python and bash helper scripts for a total of X lines)
    # unit tests (though it did fine without them for 7 years)
# instead of just exiting and informing when input files have problems, GToTree2 fixes them
    # e.g., duplicate entries will be unique'd, windows line-endings will be converted, changes written to a new file that is then used
# snakemake control over the main components for easier restart capabilities
# things i've added over time
    # gtt-gen-SCG-HMMs
    # gtt-get-GTDB-accessions
    # gtt-subset...
    # new prepackaged HMMs based on GTDB taxonomy (originals, based on NCBI, still available)
    # in tandem scanning of target KOs or pfams, files for added vizualization in iToL
    # pre-built HMMs stored remotely and downloaded when used, to slim package size and install time
    # partitions.txt file produced suitable for mixed-model treeing with programs like iqtree


### """
# In its first 7 years, the original GToTree received over 700 citations and has been
# successfully continually maintained over this time despite its foundation being a main runner
# of ~4,000 lines of BASH and dozens of independent python and BASH helper scripts with a lot
# of redundancy in its code base. Among several other improvements and feature additions, GToTree
# has now been rewritten from the ground up in python with its major stages being powered by
# Snakemake workflows - enabling the potential for resuming failed or interrupted runs. This paper highlights
# some of the new additions to GToTree since its initial launch.
### """
