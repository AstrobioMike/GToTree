##################################################################
#### GToTree v2: a user-friendlier workflow for phylogenomics ####
##################################################################

### TO DO STILL ###

    # resume functionality testing

    # do some benchmarks between gtotree v1 and v2 timing-wise, nothing extensive, just to be able to say it's like X% faster
        # on my altermonas ~90 genomes, v2 is XX minutes, v1 is YY minutes (~40% or 1.6x faster)
        # check a larger number of genomes also
        # if i want to go crazy, i could benchmark cpu/memory/time like i did for gen-mg


# gtotree2 vibrionaceae example
# mtime gtotree -w Vibrionaceae --exclusion-list vibro-exclusion-list.txt -f vibrio-bins.txt --add-gtdb-tax -L genus,species -j 15 -o vibrio-gtotree

# Time: 1:27:09
# Mem: 3576.11 MB
# CPU: 1424%

# gtotree v1 with the same HMM targets
# mtime GToTree -a vibrio-accs.txt -f vibrio-bins.txt -D -L Genus,Species -j 15 -o old-vibrio-gtotree -H /Users/michael.lee/miniconda3/envs/gtotree-dev/share/gtotree/hmm_sets/Gammaproteobacteria.hmm
# Time: 1:58:13
# Mem: 3463.40 MB
# CPU: 1337%


# gtotree2 alteromonas example (~80 genomes; ~100 target genes -> gammaproteo set)
# mtime gtotree -w alteromonas -o alteromonas-v2 -F -j 15

# Time: 3:35.28
# Mem: 2118.34 MB
# CPU: 1178%

# gtotree v1 alteromonas test (with the same HMM targets)
# mtime GToTree -a alteromonas-accs.txt -o alteromonas-v1 -F -j 15 -H /Users/michael.lee/miniconda3/envs/gtotree-dev/share/gtotree/hmm_sets/Gammaproteobacteria.hmm
# Time: 9:30.24
# Mem: 1297.80 MB
# CPU: 1121%



# gtotree2 ToL example (defaults to --derep-rank class, which was 493 genomes, i might try a --derep-rank order example too)
# mtime gtotree -w all --source ncbi -j 15 -o tol-v2 -G 0.3
# Time: 1:00:00
# Mem: 4069.82 MB
# CPU: 152%

# gtotree v1 ToL example
# mtime GToTree -a tol-accs.txt -H Universal -j 15 -o tol-v1 -F -G 0.3
# Time: 1:02:30
# Mem: 919.53 MB
# CPU: 192%



# gtotree2 bacteria/archaea example (630 out of 645 genomes; 16 target genes)
# mtime gtotree -w all -j 15 -o ba-v2 -H Universal -G 0.4
# Time: 9:50.88
# Mem: 1832.98 MB
# CPU: 989%


# gtotree v1 bacteria/archaea example (630 out of 645 genomes; 16 target genes)
# tail -n +2 ba-v2/genomes-summary-info.tsv | cut -f 1 > ba-accs.txt
# mtime GToTree -a ba-accs.txt -o ba-v1 -H /Users/michael.lee/miniconda3/envs/gtotree-dev/share/gtotree/hmm_sets/Universal-Hug-et-al.hmm -G 0.4





################################################
### other things to do prior to final launch ###
################################################

    # think about bioconda recipe, that will still need to be maintained separately from the conda-recipe in this repo
    # revisit all pages on the gtotree wiki and update them as needed
        # highlight the new functionality like --wanted-ref-tax and new subcommands
    # sync the CHANGELOG.md overall with the latest in the master gtotree v1 branch
    # make the CHANGELOG.md v2 section (and release notes) really comprehensive about the v1 to v2 changes, and put a link to the initial release notes somewhere on the wiki maybe


########################################
### Overview of change from v1 to v2 ###
########################################

### v2 HIGHLIGHTS ###
# - can specify reference genomes to include by taxonomy instead of needing to gather your own accessions now (e.g., `--wanted-ref-tax "bacteria"` or `--wanted-ref-tax "pseudomonadota"`)
# - many more prepackaged SCG sets for differet groups (run `gtt hmms` to see them)
# - HMM can be auto-selected based on the `--wanted-ref-tax` specification
# - resume interrupted runs with `--resume`
# - much fewer printouts while running and more informative progress indicators
# - all helpers are subcommands under `gtt` now (run `gtt` or `gtt -h` for an overview)
    # - this includes `gtt update-headers`, allowing you to modify headers of a previously completed run!
# - itol-compatible annotation files produced automatically for any target KOs/pfams, and for input source (so you can, e.g., easily highlight all your MAGs in a ref tree)
    # `gtt itol` helper exists for creating more itol annotation files
# - main program callable with lowercase `gtotree` or original `GToTree` now


### ADDED ###

# --wanted-ref-tax parameter added to main `gtotree` program and to `gtt gen-scg-hmms` subcommand
    # this allows specifying a taxon at any rank in order to make a tree of genomes from that taxon
    # this is so the user no longer needs to collect their own accession input lists anymore
# added a gene-represenation cutoff (-g) parameter
    # this is the minimum proportion of genomes required to have hits to a target gene for that target gene to be retained for downstream processing
    # e.g., with 100 input genomes and a -g of the default 0.2, any target gene that has hits in less than 20 genomes will be filtered out
    # This is calculated based on the total number of genomes remaining
    #   after the genome-processing steps, but before genome-level filtering based on the '-G'
    #   parameter, and it is not revisited after genome-level filtering (so as to
    #   avoid iterative pruning effects between the '-g' and '-G' parameters)
# --resume flag added to attempt to continue interrupted runs
# helper programs are now subcommands under `gtt`
    # run `gtt` by itself or `gtt -h` for an overview

# lots of things since initial v1 release, but were integrated before v2

### CHANGED ###

# command callable with lowercase `gtotree` or original `GToTree` now (i don't know what i was thinking almost a decade ago)
# the code-base has been virtually entirely rewritten from a mix of bash and python to just python
# when calculating percent of target genes required to be found (-X parameter)
    # now it uses the amount remaining after filtering out target genes with too few genomes having hits as the denominator
    # previously it used the starting total as the denominator
        # example: if starting with 100 target genes, and 20 are filtered out for having hits in too few genomes, then the percent required is based on 80 target-genes now rather than 100
        # this ensures the percentage of genes that end up no the tree is what is filtered on
# default number of jobs set to 4 instead of 1 (-j parameter)
# default output changed to "gtotree-output"
# dropped '-n' num HMM cpus argument (for scanning individual genomes, the default of 2 is virtually always fine)
# default lineage settings are D,P,C,S (no longer strain)
# printed output has more progress bars and fewer text printouts
# better logging of many steps
# http is used instead of ftp always now
# output files and dirs that had underscores have had them replaced with dashes (sorry, i now hate underscores when they're not necessary)
# estimated percent completion and redundancy are no long provided
    # (this was always just "free" info, but checkm2 is a much better way to do this, so i made the decision to stop providing it at all)
# taxids are no longer searched for in input genbank files, they will only be found/used for provided input ncbi-accessions
    # due to the nature of genbank files, this was always a little messy and hacky. If this is actually a feature that benefitted anyone, i'll try adding it back in upon request :)
# there are no longer separate summary tables for each input-genome source (accessions, fasta files, genbank files, and amino-acid files)
    # they are all represented in the primary "genomes-summary-info.tsv" table, which includes a "source" column
# references are slimmed down and hosted on GToTree's (or bit's) github as assets now, this will help users who had trouble accessing ftp or other sites due to firewalls or other restrictions

### IMPROVEMENTS to highlight in paper ###

# users no longer need to gather their own input accessions, they can just specify a taxon and GToTree will get the accessions then ref genomes for them
# python packaging and structure (rather than 4,000 lines of bash as main runner with dozens of individual python and bash helper scripts for a total of X lines)
    # unit tests (though it did fine without them for 7 years)
# instead of just exiting and informing when input files have problems, GToTree2 fixes them
    # e.g., duplicate entries will be unique'd, windows line-endings will be converted, changes written to a new file that is then used
# things i've added over time
    # gtt gen-SCG-HMMs
    # gtt get-accs-from-gtdb
    # gtt get-accs-from-ncbi
    # gtt search-pfams
    # gtt search-kos
    # new prepackaged HMMs based on GTDB taxonomy (originals, based on NCBI, still available)
    # in tandem scanning of target KOs or pfams, files for added vizualization in iToL
    # partitions.txt and partitions.nex files produced suitable for mixed-model treeing with programs like iqtree
    # auto-generation of citations.txt of all tools used in a run


## Facilitating citing all tools
    # citations.txt (find what year I added this to GToTree and note it)
        # example output from a run



### """
# In its first 7 years, the original GToTree received over 700 citations and has been
# successfully continually maintained over this time despite its foundation being a main runner
# of ~4,000 lines of BASH and dozens of independent python and BASH helper scripts with a lot
# of redundancy in its code base. Among several other improvements and feature additions, GToTree
# has now been rewritten from the ground up in python with robustification and restart capabilities
# for failed or interrupted runs. This paper highlights some of the many new additions to GToTree
# since its initial launch.
### """



#### paper examples
    # new tree of life
    # nifH in Cyanos


#### paper discussion
    # "Where does this fit in with your analysis?"
        # MAG recovery -> taxonomic classification -> tree (--wanted-ref-tax, -f <list-of-my-mags>)
        # I want a tree of Alteromonas (--wanted-ref-tax Alteromonas)



##### accs that don't have enough hits for random testing #####

cat problem-accs.txt
# GCA_902755425.1
# GCA_902785715.1
# GCA_902777105.1
# GCA_902765875.1
# GCA_016291275.1
