# This directory holds the tooling and the record for deciding which single-copy-gene HMM sets GToTree prepackages, based on GTDB
# latest run was with gtdb release 232
# needs to be run in active gtotree v2+ env


# this counts taxa and chooses which will be prepackaged (it reads the scg-sets.toml)
python plan-scg-sets.py

# this builds them all, produced by above
bash plan/build-scg-sets.sh

# this creates the hmm-sources-and-info.tsv and commands to upload them to my premade github release that will hold them
python publish-scg-sets.py --build-root scg-set-builds --release-tag gtdb-scg-sets-r232 --gtdb-release r232

